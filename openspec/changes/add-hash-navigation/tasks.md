## 1. Реализация роутера

- [ ] 1.1 Создать `src/lib/router.js` с реактивным стором хэш-маршрутизации, нормализацией хэша, методами `navigate`, `replace`, `back` и слушателями событий (`hashchange`, `popstate`, кнопка мыши 3), проверив корректность обновления хэша и маршрута.

## 2. Интеграция с компонентами

- [ ] 2.1 Перевести `src/lib/ExtensionApp.svelte` на использование `$route` из `src/lib/router.js`, удалив локальный `routeStack`, проверив синхронизацию отображения экранов с URL-хэшем.
- [ ] 2.2 Обновить вызовы навигации и кнопку «Назад» в `PageFrame.svelte`, `TranslatePage.svelte` и `SettingsPage.svelte` на методы роутера, проверив корректность возврата назад и fallback.

## 3. Проверка и сборка

- [ ] 3.1 Протестировать сценарии навигации между всеми экранами (перевод, результат, история, избранное, настройки, обратная связь), а также работу кнопок «Назад» браузера и мыши.
- [ ] 3.2 Выполнить `npm run build` и убедиться в успешной сборке проекта без ошибок компиляции и сборщика.
